apiVersion: v1
kind: Service
metadata:
  name: dashboard-kubernetes
  namespace: dashboard
  labels:
    app.kubernetes.io/name: kubernetes-dashboard
spec:
  type: ClusterIP
  ports:
  - port: 443
    targetPort: 8443
    protocol: TCP
    name: https
  selector:
    app.kubernetes.io/name: kubernetes-dashboard

