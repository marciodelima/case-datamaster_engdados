apiVersion: v1
kind: Service
metadata:
  name: airflow-web
  namespace: airflow
spec:
  selector:
    app: airflow-web
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: airflow
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: airflow-web
  endpoints:
    - port: metrics
      interval: 30s

